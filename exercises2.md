### **Question:**  
Write a Python function called `count_vowels(text)` that returns the number of vowels (`a, e, i, o, u`) in a given string.
The function should:

- Treat uppercase and lowercase letters as the same
- Ignore non‑alphabetic characters
- Return an integer representing the total vowel count

### **Example:**  
```python
count_vowels("Future Focus Academy")  # Output: 8
```

---