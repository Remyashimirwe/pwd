## 🚗 Question 1: Car Rental System

Create a class `Car` with:

* A class property `company_name`
* Instance properties: `brand`, `model`, `price_per_day`, `is_available`

### Tasks:

1. Create two car objects
2. Create a method `rent_car()` that changes availability
3. Create a method `return_car()`
4. Print the company name using one object


---

## 🧑‍🎓 Question 2: Student System

Create a class `Student` with:

* A class property `school_name`
* Instance properties: `name`, `grade`

### Tasks:

1. Create one student
2. Dynamically add `email` and `age`
3. Print all properties
4. Delete `age` and try printing it again
---

## 🛒 Question 3: Product Store

Create a class `Product` with:

* A class property `currency`
* Instance properties: `name`, `price`

### Tasks:

1. Create two products
2. Change the price of one product
3. Change the currency for all products
4. Print both products


---

## 🏥 Question 4: Patient Records

Create a class `Patient` with:

* A class property `hospital_name`
* Instance properties: `name`, `_age`

### Tasks:

1. Create a method `get_age()`
2. Create a method `set_age(new_age)`
3. Prevent age from decreasing
4. Test with valid and invalid updates

---

## 📦 Question 5: Inventory Counter

Create a class `Item` with:

* A class property `total_items = 0`
* Instance property: `name`

### Tasks:

1. Increment `total_items` every time an object is created
2. Create 3 items
3. Print total items


