Write a Python function called process_numbers(nums) that takes a list of integers and returns a dictionary with the following keys:

"even" → a list of all even numbers

"odd" → a list of all odd numbers

"squares" → a list containing the square of each number

"greater_than_10" → a list of numbers greater than 10

"lambda_sum" → the sum of all numbers using a lambda + reduce

"iterator_first_three" → the first three items using an iterator (not slicing)

Additional rules:

You must use:

a for loop

an if/elif structure

a lambda function

an iterator created with iter()

Do not use list comprehensions.

another question 

---
Write a Python function called analyze_strings(words) that takes a list of strings and returns a dictionary with the following keys:
"uppercase" → a list of all strings converted to uppercase

"long_words" → a list of strings whose length is greater than 5

"starts_with_vowel" → a list of strings that begin with a vowel

"lengths" → a list containing the length of each string

"lambda_concat" → all strings concatenated together using a lambda + reduce

"iterator_two_items" → the first two items using an iterator (not slicing)

Rules you must follow:
You must use a for loop

You must use an if/elif structure

You must use a lambda function

You must use an iterator created with iter()

You must NOT use list comprehensions


---
**Write a Python function called `process_temperatures(temps)` that takes a list of integers representing temperatures and returns a dictionary with the following keys:**

- **"above_freezing"** → a list of temperatures greater than 0  
- **"below_or_equal_freezing"** → a list of temperatures less than or equal to 0  
- **"converted_to_fahrenheit"** → a list of temperatures converted to Fahrenheit using the formula \( F = C × 9/5 + 32 \)  
- **"high_temps"** → a list of temperatures greater than 30  
- **"lambda_product"** → the product of all temperatures using a lambda + `reduce`  
- **"iterator_first_four"** → the first four temperatures using an iterator (not slicing)

---

### **Rules you must follow:**

- You must use a **for loop**  
- You must use an **if/elif structure**  
- You must use a **lambda function**  
- You must use an **iterator created with `iter()`**  
- You must **NOT** use list comprehensions  

---